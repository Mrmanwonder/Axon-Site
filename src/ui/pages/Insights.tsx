import RouteStub from "../components/RouteStub";

export default function Insights() {
  return (
    <RouteStub
      title="Insights"
      note="Deep dive across every scanned paper. Renders the honest 'not enough papers yet' state below the readiness threshold — that is a state of this route, not a route of its own. Screen content is ported in the next pass."
    />
  );
}
