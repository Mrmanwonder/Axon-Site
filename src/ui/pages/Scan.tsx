import RouteStub from "../components/RouteStub";

export default function Scan() {
  return (
    <RouteStub
      title="Scan"
      note="Upload-first ingestion. No in-app camera in v1. This flow gets zero decorative motion. Screen content is ported in the next pass."
    />
  );
}
