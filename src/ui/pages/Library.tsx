import RouteStub from "../components/RouteStub";

export default function Library() {
  return (
    <RouteStub
      title="Library"
      note="The archive: every paper, searchable and filterable. Screen content is ported in the next pass."
    />
  );
}
